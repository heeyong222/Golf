<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.golfzon.golfzoin.mapper.ChatMapper">
	<select id="chatList" parameterType="int" resultType="MessageDto">
		select 
			* 
		from 
			dbo.golfzoin_chatlog with(nolock)
		where 
			roomNo = #{roomNo}
		order by 
			date
	</select>
	<insert id="pushchat" parameterType="map">
		insert into 
			dbo.golfzoin_chatlog(
				roomNo, 
				author, 
				data, 
				date, 
				profile
				)
		values(
			CONVERT(int, #{roomNo}), 
			#{author}, 
			#{data}, 
			GETDATE(),
		<if test='profile == null'>
			null
		</if>
		<if test='profile != null'>
			#{profile}
		</if>
		)
	</insert>
</mapper>